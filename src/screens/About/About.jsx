import React, { memo } from 'react';
import Layout from '../../components/shared/Layout/Layout'

const About = memo(() => {
  return (
    <Layout>
    <div className="home">
     
    </div>
  </Layout>
  );
});

export default About;